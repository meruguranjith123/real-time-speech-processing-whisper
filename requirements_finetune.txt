# Additional requirements for fine-tuning Whisper model
# Install with: pip install -r requirements_finetune.txt

# Core fine-tuning libraries
transformers>=4.35.0
peft>=0.6.0
accelerate>=0.24.0
datasets>=2.14.0
tqdm>=4.66.0

# Existing dependencies (also needed)
torch>=2.0.0
torchaudio>=2.0.0
numpy>=1.24.0





